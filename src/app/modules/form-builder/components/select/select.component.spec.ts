// import { async, ComponentFixture, TestBed } from '@angular/core/testing';

// import { SelectComponent } from './select.component';

// describe('SelectComponent', () => {
//   let component: SelectComponent;
//   let fixture: ComponentFixture<SelectComponent>;

//   beforeEach(async(() => {
//     TestBed.configureTestingModule({
//       declarations: [ SelectComponent ]
//     })
//     .compileComponents();
//   }));

//   beforeEach(() => {
//     fixture = TestBed.createComponent(SelectComponent);
//     component = fixture.componentInstance;
//     fixture.detectChanges();
//   });

//   it('should create', () => {
//     expect(component).toBeTruthy();
//   });


  // it(`should call the delete method`, async(() => {
  //   // component.registerForm.setValue({ firstName: 'kshah', lastName: 'shah', email: 'kscn@gmail.com' });
  //   spyOn(component, 'deleteFieldValue');
  //   fixture.detectChanges();
  //   el = fixture.debugElement.query(By.css('button')).nativeElement;
  //   el.click();
  //   expect(component.deleteFieldValue).toHaveBeenCalled();
  // }));
  // it(`should have as title 'dynamic-field-demo'`, () => {
  //   const fixture = TestBed.createComponent(AppComponent);
  //   const app = fixture.debugElement.componentInstance;
  //   expect(app.title).toEqual('dynamic-field-demo');
  // });

  // it('should render title', () => {
  //   const fixture = TestBed.createComponent(AppComponent);
  //   fixture.detectChanges();
  //   const compiled = fixture.debugElement.nativeElement;
  //   expect(compiled.querySelector('.content span').textContent).toContain('dynamic-field-demo app is running!');
  // });
// });
